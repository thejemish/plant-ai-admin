# MobileCLIP-S0 Model Files

Place the exported MobileCLIP-S0 image encoder here for admin-side embedding jobs.

Expected default path:

```text
admin/models/mobileclip-s0/vision_model.onnx
```

Model binaries are ignored by git because they are large runtime artifacts.
